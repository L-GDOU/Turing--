考虑到以及获取到33个关键点 首先考虑到直接考虑left_feet_index（后续简称lfi）关键点
在人跑步过程中 一般属于前脚掌着地 而lfi轨迹可近似成椭圆 同时 跑步过程中 每一秒至多只有一只脚着地
于是可以将模型抽象成一个单轮车 于是速度可以抽象成lfi的线速度
在微分思想下 使用last记录上一帧位置 通过两位置的像素间隔 再通过一定比例放大 即可得到相对位移再除以时间间隔即可得到速度
在实际操作过程中可以发现 lfi有部分时间并不能完全反映速度 于是选择将关注的关键点替换为left_knee（后续简称lk）
在实际跑步过程中 速度应为一只脚向后速度加上一只脚向前速度之和 所有最后应对得到的速度乘2
在计算位移时 通过身高在整个图像高度的占比约为6/7 为方便计算 取身高等于1.8米 可计算得到图像相对位移与实际位移比例系数大小约为3.7
